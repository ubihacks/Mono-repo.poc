const baseStyle = {
	label: {
		color: 'muted',
		fontWeight: 'medium',
	},
};

const sizes = {
	md: {
		label: {
			fontSize: 'sm',
		},
	},
};

const defaultProps = {
	colorScheme: 'purple',
};

export default {
	baseStyle,
	sizes,
	defaultProps,
};
